import { ToNativeDateDirective } from './to-native-date.directive';

describe('ToNativeDateDirective', () => {
  it('should create an instance', () => {
    const directive = new ToNativeDateDirective();
    expect(directive).toBeTruthy();
  });
});
